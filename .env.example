# Kickstarter AI Scraper - Environment Variables
# Copy to .env and fill in values

# Rate limiting (requests per second) - be polite
RATE_LIMIT_RPS=1.0

# Optional: proxy URL for rotation
# HTTP_PROXY=http://user:pass@proxy:port

# Data directories (defaults shown)
# DATA_DIR=./data
# LOG_DIR=./logs
